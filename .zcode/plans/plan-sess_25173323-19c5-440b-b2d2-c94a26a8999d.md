# Plan: close the six gaps (G1–G6) before OpenWaggleMain consumes the harness

All edits are to existing files at confirmed line numbers. Deliberate deviations (model-driven log *injection*, no auto-build, design-skill-as-helper) are left untouched per your call.

---

## G4 — skip inspiration/assets/design-skill for non-UI/backend projects

**Thread the live project category into the loop.** The category lives on `Session.memory` and is *corrected* after Prepare (`session.ts:538`), so a static config snapshot is wrong. Make it a mutable field.

1. `src/orchestrator/orchestrator.ts` (~class fields near :581): add `projectCategory?: ProjectCategory;` (mutable, not on `config`).
2. `src/session.ts` (~:244 at `new Orchestrator(...)`): seed it from `opts.preset`; in `reconcileAfterPrepare` (~:538) call `this.orchestrator.projectCategory = corrected` so the post-Prepare correction is seen live.
3. `src/orchestrator/loop.ts`: add `projectCategory?: ProjectCategory;` to `ToolLoopInput` (:66).
4. `src/orchestrator/orchestrator.ts`: pass `...(this.projectCategory ? { projectCategory: this.projectCategory } : {})` at the plan (:1021) and work-step (:1109) `runToolLoop` sites (verify/conversational don't author UI).
5. **The gate** — `loop.ts:724` `isFreshUiBuild`: add `&& input.projectCategory !== "backend"` so the auto-invoke ladder (inspiration → design-skill) is skipped for backend projects.
6. **Also cover model-invoked calls** (the ladder only handles auto-invoke): add `projectCategory?` to `ToolContext` (`types.ts:380`), set it in `toolCtx` (`loop.ts:853`), and make `inspiration_generator` and `assets_generator` decline with a friendly note when `ctx.projectCategory === "backend"`. This closes both paths.

## G6 — enforce `__t()` probe removal before completion

The `instrumentedFiles: Set` at `activity-monitor.ts:63` is **dead** (never populated; the provider can't observe the model's `edit` calls), so cleanup can't list files and nothing strips markers today. Enforcement goes in the orchestrator.

1. **Make the loop record instrumented paths.** In `loop.ts`, when a `write`/`edit` result's content/diff contains a probe marker (`__t(`, `__TRACE`, `_TRACE_ID`, `__traceId`, `__TRACE_ID`), add the path to a new `instrumentedPaths: string[]` on `ToolLoopResult` (mirror `writtenPaths`).
2. **Orchestrator post-verify scan.** After the verify loop (`orchestrator.ts:1277`, before the summary at :1288): cheap `fs` read of each path in `instrumentedPaths` (collected across steps) for the marker tokens. If any remain, push **one** extra verify round (round-capped, can't deadlock) with a targeted message naming the files: "strip the `__t()`/`__TRACE` instrumentation you added." Re-scan; only proceed to summary when clean.
3. **Resurrect the dead branch (optional, low cost):** thread the loop's `instrumentedPaths` into `activity_cleanup` so its "Remove the `__t()` calls from:" message (:1335) actually lists files instead of always being empty. Pass `ctx` into `cleanupAction` (`activity-monitor.ts:933`).

## G2 + G3 — media_analysis category + OCR, injected into follow-up calls

**G2 — give media_analysis a triage category and structured OCR (zero extra model calls):**
1. `src/tools/builtin/media-analysis.ts:51` extend `MediaAnalysisResult`: add `category?: "informational" | "ui-replicate" | "ui-bug" | "other"` and `ocr?: { text: string }`.
2. Fold a final line into every `LENS_SYSTEM` prompt (:190–256): `CATEGORY: <informational|ui-replicate|ui-bug|other>`. Parse + strip it from the visible `analysis` before the return at :696; set `details.category`.
3. When `lens === "ocr"`, also set `details.ocr = { text: analysis }` (keep the verbatim-text contract intact).

**G3 — make the triage reach follow-up tool calls (two parts):**
- **(3a) Upfront triage pre-pass** (spec: "when attachments → media analysis to get ocr + category → include in harness so follow-up calls get this data"). In the orchestrator before the work loops, when `opts.images?.length` is set, run one cheap flash-model `media_analysis` triage per attached image. Results: (i) augment the "AVAILABLE IMAGES" list (`orchestrator.ts:1362` & `:1415`; widen the entry type at `:219` with optional `category?`/`ocr?`/`note?`) so the model knows each image's role without re-analyzing; (ii) for `informational` images with OCR, fold the text into the loop's carried `mediaFact`. Guard with a new `HarnessConfig.autoTriageAttachments` (default `true`) so OpenWaggleMain can disable it.
- **(3b) Loop-carried mediaFact** (for model-invoked mid-run analysis): mirror the existing `designReference` pattern exactly — add `let mediaFact: string | undefined` at `loop.ts:289`; capture from a `media_analysis` result near `:976`; add `mediaFact?` to `AuthoringContext` (`types.ts:264`) and inject it into `authoringContext` at `loop.ts:848` so write/edit authoring sees it.

## G5 — add `fonts` to the design blueprint

1. `src/tools/builtin/inspiration-generator.ts:100` add `fonts?: Record<string, unknown>` to `InspirationJson`; render it in `summarizeSections` (:324). (Backend won't populate it — noted in the existing comment at :92 — so this primarily benefits the design-skill path the harness owns.)
2. `src/tools/builtin/design-skill.ts`: add a `"fonts": ...` line to the `DESIGN_SKILL_SYSTEM` shape sketch (:94–107) and `fonts?` to `DesignedSection` (:56). The `parseSections` (:263) already spreads the whole object, so **no parser change**.
3. **No authoring change** — `authoring.ts:570/:602` `JSON.stringify` the whole `designReference`, so `fonts` renders automatically. Caveat: to respect the existing ROLE-not-literal discipline (`authoring.ts:90` lists fonts as do-not-take), the design-skill prompt will emit font *roles* (e.g. `{"heading":"display","body":"text"}`), not family names.

## G1 — classifier DEBUGGING hint (host flag still wins if explicitly set)

Per your choice ("Add classifier hint" — host flag wins if explicitly set).

1. `src/phases/prompts.ts:1299` `INTENT_ROUTER_PROMPT`: relax the single-word contract to two lines — `ROUTE: TASK|CONVERSATIONAL` and `BUGFIX: YES|NO` (YES when the task is fixing a bug/crash/failure in existing code).
2. `src/orchestrator/orchestrator.ts:834` `classifyIntent`: widen return to `Promise<{ route: ChainRoute; bugFixHint?: boolean }>`; parse both lines. Update both call sites (`run`:932, `runChain`:1633).
3. **Resolution** at `orchestrator.ts:965`: `const isBugFix = opts.isBugFix ?? hint ?? false;` — explicit host `true`/`false` wins; unset consults the hint. (This is the `??` semantics, matching "host flag wins *if explicitly set*"; `=== true || hint` would let a hint override an explicit `false`, which is not what you chose.)

---

## Files touched (summary)
- `src/types.ts` — `ToolContext.projectCategory`, `AuthoringContext.mediaFact`.
- `src/orchestrator/loop.ts` — `ToolLoopInput.projectCategory`; `mediaFact` accumulator; capture from media_analysis; `instrumentedPaths`; `isFreshUiBuild` backend gate; `toolCtx.projectCategory`.
- `src/orchestrator/orchestrator.ts` — `projectCategory` live field; thread into `runToolLoop` sites; upfront attachment triage pre-pass + `autoTriageAttachments`; post-verify `__t()` scan + extra round; `classifyIntent` widening + `?? hint` resolution; AVAILABLE IMAGES category rendering.
- `src/session.ts` — seed/update orchestrator category.
- `src/tools/builtin/media-analysis.ts` — category + ocr in result; prompt fold + parse.
- `src/tools/builtin/inspiration-generator.ts` — `fonts` field + summary; backend-decline on `backend`.
- `src/tools/builtin/assets-generator.ts` — backend-decline on `backend`.
- `src/tools/builtin/design-skill.ts` — `fonts` in prompt shape + type.
- `src/phases/prompts.ts` — `INTENT_ROUTER_PROMPT` two-line contract.
- `src/harness.ts` — `autoTriageAttachments` config plumbing (if not already threaded).

## Tests
Add/extend `.mjs` tests: classifier returns `bugFixHint`; `isBugFix` resolution matrix (host true/false/unset × hint); `isFreshUiBuild` false for `backend`; assets/inspiration decline for backend; media_analysis parses category + ocr; loop carries `mediaFact` into authoringContext; post-verify marker scan forces a round when `__t()` remains. Run the existing `tests/*.test.mjs` suite to confirm no regressions.

## Order & risk
G4 → G5 → G1 → G2 → G3 → G6 (lowest-risk, most isolated first). All changes are additive with defaults preserving current behavior; the `isBugFix ?? hint` and `autoTriageAttachments` knobs let OpenWaggleMain opt out. No schema/migration; the inspiration backend's `fonts` absence is already documented.